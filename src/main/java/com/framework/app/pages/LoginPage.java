package com.framework.app.pages;

public class LoginPage {
	
	
	public void login(String userName, String password) {
		/*
		 * driver.find(By.xpath("")).sendkeys(userName);
		 * driver.find(By.xpath("")).sendkeys(password); driver.find.click;
		 */
	}
	
	
	

}
